## 全面日期字段检查结果

### 问题总结
通过对代码库的全面检查，发现以下文件存在 `start_date` 和 `end_date` 字段名使用问题：

1. **backend\core\project_service.py**
   - `_create_task` 方法：使用了 `start_date` 和 `end_date` 字段
   - `update_task` 方法：检查了 `start_date` 和 `end_date` 字段

2. **backend\api\chat.py**
   - 第 594-595 行：使用了 `data.get("start_date")` 和 `data.get("end_date")`
   - 第 610-611 行：使用了 `data.get("start_date")` 和 `data.get("end_date")`

3. **backend\models\schemas.py**
   - 项目相关字段使用了 `start_date` 和 `end_date`

4. **backend\api\project.py**
   - 项目相关字段使用了 `start_date` 和 `end_date`

## 修复计划

### 1. 修复 project_service.py

#### _create_task 方法 (第 295-298 行)
**问题**：使用了 `task_data.get("start_date")` 和 `task_data.get("end_date")`
**修复**：改为使用 `task_data.get("planned_start_date")` 和 `task_data.get("planned_end_date")`

#### update_task 方法 (第 406-411 行)
**问题**：检查了 `start_date` 和 `end_date` 字段
**修复**：改为检查 `planned_start_date` 和 `planned_end_date` 字段

### 2. 修复 chat.py

#### 第 594-595 行
**问题**：使用了 `data.get("start_date")` 和 `data.get("end_date")`
**修复**：改为使用 `data.get("planned_start_date")` 和 `data.get("planned_end_date")`

#### 第 610-611 行
**问题**：使用了 `data.get("start_date")` 和 `data.get("end_date")`
**修复**：改为使用 `data.get("planned_start_date")` 和 `data.get("planned_end_date")`

### 3. 修复 schemas.py

**项目相关字段**：保持 `start_date` 和 `end_date` 不变（项目级别的字段）
**任务相关字段**：确保使用 `planned_start_date` 和 `planned_end_date`

### 4. 修复 api\project.py

**项目相关字段**：保持 `start_date` 和 `end_date` 不变（项目级别的字段）
**任务相关字段**：确保使用 `planned_start_date` 和 `planned_end_date`

## 预期修复效果

修复后，整个代码库将：
1. **统一使用正确的字段名**：任务级别的日期字段使用 `planned_start_date` 和 `planned_end_date`
2. **消除字段名混淆**：不再混用 `start_date`/`end_date` 和 `planned_start_date`/`planned_end_date`
3. **保证数据一致性**：前端传递的字段能够被后端正确识别和处理
4. **修复日期更新功能**：任务日期更新将正常工作，不再出现 null 值的问题

## 实施步骤

1. **修改 project_service.py**：更新字段名检查逻辑
2. **修改 chat.py**：更新数据获取逻辑
3. **验证修复**：运行测试确保功能正常
4. **提交代码**：完成修复并推送到 GitHub