# 项目管理助手机器人 - 前端UI/UX设计文档

## 1. 整体布局设计

### 1.1 主界面布局（三栏式布局）

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│  [Logo] 项目管理助手                    [项目列表▼]  [设置]  [帮助]  [用户头像]   │  ← Header (60px)
├───────────────┬───────────────────────────────────────────┬─────────────────────┤
│               │                                           │                     │
│   聊天区域    │           项目信息/甘特图区域              │    详情侧边栏       │
│   (350px)     │           (自适应宽度)                     │    (300px)          │
│               │                                           │                     │
│ ┌───────────┐ │ ┌───────────────────────────────────────┐ │ ┌─────────────────┐ │
│ │ AI消息    │ │ │                                       │ │ │ 项目详情        │ │
│ │ [头像]    │ │ │     项目甘特图 / 任务列表              │ │ │ ─────────────── │ │
│ │ 你好！我  │ │ │                                       │ │ │ 项目名称: XXX   │ │
│ │ 是您的... │ │ │  [项目A] [项目B] [项目C] [+新建]      │ │ │ 负责人: 张三    │ │
│ │           │ │ │                                       │ │ │ 进度: 75%       │ │
│ └───────────┘ │ │  ┌─────────────────────────────────┐  │ │ │ 状态: 进行中    │ │
│               │ │  │                                 │  │ │ │                 │ │
│ ┌───────────┐ │ │  │      甘特图/表格视图切换         │  │ │ 子任务列表      │ │
│ │ 用户消息  │ │ │  │                                 │  │ │ │ ─────────────── │ │
│ │ 帮我查看  │ │ │  │  [周视图] [月视图] [季视图]      │  │ │ │ □ 任务1 (50%)   │ │
│ │ 项目进度  │ │ │  │                                 │  │ │ │ □ 任务2 (80%)   │ │
│ └───────────┘ │ │  │  ═══════════════════════════    │  │ │ │ □ 任务3 (100%)  │ │
│               │ │  │  │任务A│██████░░░░│            │  │ │ │                 │ │
│ ┌───────────┐ │ │  │  │任务B│████████░░│            │  │ │ 交付物          │ │
│ │ AI消息    │ │ │  │  │任务C│██████████│            │  │ │ │ ─────────────── │ │
│ │ [头像]    │ │ │  │  ═══════════════════════════    │  │ │ │ 📄 需求文档.pdf │ │
│ │ 当前有3   │ │ │  │                                 │  │ │ │ 📊 进度报告.xlsx│ │
│ │ 个项目... │ │ │  └─────────────────────────────────┘  │ │ └─────────────────┘ │
│ └───────────┘ │ │                                       │ │                     │
│               │ │  [切换视图: 甘特图 | 列表 | 看板]      │ │                     │
│ ┌───────────┐ │ │                                       │ │                     │
│ │ [🎤]      │ │ └───────────────────────────────────────┘ │                     │
│ │ 输入消息..│ │                                           │                     │
│ │ [发送]    │ │                                           │                     │
│ └───────────┘ │                                           │                     │
│               │                                           │                     │
└───────────────┴───────────────────────────────────────────┴─────────────────────┘
```

### 1.2 响应式布局断点

| 断点 | 宽度范围 | 布局调整 |
|------|----------|----------|
| Desktop XL | ≥1440px | 三栏完整显示 |
| Desktop | 1024px-1439px | 隐藏右侧详情栏，点击展开 |
| Tablet | 768px-1023px | 双栏布局，聊天可收起 |
| Mobile | <768px | 单栏，底部Tab切换 |

### 1.3 配置界面布局

```
┌─────────────────────────────────────────────────────────────────┐
│  [Logo] 项目管理助手                    [返回主界面]            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    系统配置                              │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │                                                         │   │
│  │  LLM提供商                                              │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐                   │   │
│  │  │ OpenAI  │ │  Kimi   │ │  豆包   │                   │   │
│  │  │   ●     │ │    ○    │ │    ○    │                   │   │
│  │  └─────────┘ └─────────┘ └─────────┘                   │   │
│  │                                                         │   │
│  │  API配置                                                │   │
│  │  ┌─────────────────────────────────────────────────┐   │   │
│  │  │ Base URL                                        │   │   │
│  │  │ [https://api.openai.com/v1              ]       │   │   │
│  │  └─────────────────────────────────────────────────┘   │   │
│  │  ┌─────────────────────────────────────────────────┐   │   │
│  │  │ API Key                                         │   │   │
│  │  │ [sk-************************************]       │   │   │
│  │  └─────────────────────────────────────────────────┘   │   │
│  │  ┌─────────────────────────────────────────────────┐   │   │
│  │  │ Model 名称                                      │   │   │
│  │  │ [gpt-4-turbo                              ] [▼] │   │   │
│  │  └─────────────────────────────────────────────────┘   │   │
│  │                                                         │   │
│  │  高级设置                                                │   │
│  │  ┌─────────────────────────────────────────────────┐   │   │
│  │  │ Temperature: [0.5 ----●---- 1.0]                │   │   │
│  │  │ Max Tokens:  [2000    ]                         │   │   │
│  │  └─────────────────────────────────────────────────┘   │   │
│  │                                                         │   │
│  │  ┌─────────────┐  ┌─────────────┐                      │   │
│  │  │  测试连接   │  │  保存配置   │                      │   │
│  │  └─────────────┘  └─────────────┘                      │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. 组件结构图

### 2.1 整体组件层级

```
App
├── Layout
│   ├── Header
│   │   ├── Logo
│   │   ├── ProjectSelector
│   │   ├── NavActions (设置/帮助)
│   │   └── UserAvatar
│   ├── MainContent
│   │   ├── ChatPanel (左侧)
│   │   │   ├── ChatMessageList
│   │   │   │   └── ChatMessage (递归)
│   │   │   │       ├── MessageAvatar
│   │   │   │       ├── MessageContent (Markdown渲染)
│   │   │   │       ├── MessageActions (复制/重试)
│   │   │   │       └── MessageTimestamp
│   │   │   ├── ChatInput
│   │   │   │   ├── TextArea (自动高度)
│   │   │   │   ├── VoiceInputButton
│   │   │   │   ├── SendButton
│   │   │   │   └── FileUploadButton
│   │   │   └── TypingIndicator (流式响应)
│   │   ├── ProjectPanel (中间)
│   │   │   ├── ViewTabs (甘特图/列表/看板)
│   │   │   ├── GanttChart
│   │   │   │   ├── GanttToolbar (时间范围切换)
│   │   │   │   ├── GanttTimeline (时间轴)
│   │   │   │   └── GanttTaskBar (任务条)
│   │   │   ├── ProjectTable
│   │   │   │   ├── TableHeader
│   │   │   │   └── TableRow
│   │   │   └── KanbanBoard
│   │   │       ├── KanbanColumn
│   │   │       └── KanbanCard
│   │   └── DetailSidebar (右侧)
│   │       ├── ProjectInfoCard
│   │       ├── TaskList
│   │       └── DeliverableList
│   └── Footer (可选状态栏)
├── ConfigModal/ConfigPage
│   ├── ProviderSelector
│   ├── ApiConfigForm
│   ├── AdvancedSettings
│   └── ActionButtons
└── Toast/Notification
```

### 2.2 组件关系图

```
                    ┌─────────────────┐
                    │   App (Root)    │
                    └────────┬────────┘
                             │
              ┌──────────────┼──────────────┐
              │              │              │
              ▼              ▼              ▼
       ┌──────────┐   ┌──────────┐   ┌──────────┐
       │  Layout  │   │ Config   │   │  Toast   │
       │          │   │  Page    │   │          │
       └────┬─────┘   └──────────┘   └──────────┘
            │
    ┌───────┴───────┐
    │               │
    ▼               ▼
┌────────┐    ┌────────────┐
│ Header │    │MainContent │
└────────┘    └─────┬──────┘
                    │
        ┌───────────┼───────────┐
        │           │           │
        ▼           ▼           ▼
   ┌─────────┐ ┌──────────┐ ┌──────────┐
   │ChatPanel│ │ Project  │ │ Detail   │
   │         │ │ Panel    │ │ Sidebar  │
   └────┬────┘ └────┬─────┘ └──────────┘
        │           │
   ┌────┴────┐ ┌────┴────┐
   │         │ │         │
   ▼         ▼ ▼         ▼
┌───────┐ ┌────────┐ ┌──────────┐
│Message│ │ Gantt  │ │  Table   │
│ List  │ │ Chart  │ │          │
└───────┘ └────────┘ └──────────┘
```

---

## 3. 状态管理设计

### 3.1 全局状态 (Global State)

```typescript
// store/index.ts
interface GlobalState {
  // 用户配置
  config: {
    llmProvider: 'openai' | 'kimi' | 'doubao';
    apiConfig: {
      baseUrl: string;
      apiKey: string;
      model: string;
      temperature: number;
      maxTokens: number;
    };
  };
  
  // 当前视图状态
  ui: {
    activePanel: 'chat' | 'project' | 'config';
    projectViewMode: 'gantt' | 'table' | 'kanban';
    ganttTimeRange: 'week' | 'month' | 'quarter';
    sidebarCollapsed: boolean;
    selectedProjectId: string | null;
    selectedTaskId: string | null;
  };
  
  // 聊天状态
  chat: {
    messages: Message[];
    isStreaming: boolean;
    currentStreamContent: string;
    inputText: string;
    isRecording: boolean;
  };
  
  // 项目数据
  projects: {
    list: Project[];
    currentProject: Project | null;
    loading: boolean;
    error: string | null;
  };
}
```

### 3.2 状态流设计

```
┌─────────────────────────────────────────────────────────────────┐
│                        状态流架构                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────┐      ┌─────────────┐      ┌─────────────┐     │
│  │   用户操作   │ ───▶ │   Action    │ ───▶ │   Reducer   │     │
│  │  (UI Event) │      │  (Dispatch) │      │  (Update)   │     │
│  └─────────────┘      └─────────────┘      └──────┬──────┘     │
│                                                   │             │
│                                                   ▼             │
│  ┌─────────────┐      ┌─────────────┐      ┌─────────────┐     │
│  │   UI更新    │ ◀─── │  Selector   │ ◀─── │   Store     │     │
│  │  (Re-render)│      │  (GetState) │      │  (State)    │     │
│  └─────────────┘      └─────────────┘      └─────────────┘     │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  异步流程: Action ──▶ Middleware ──▶ API ──▶ Action     │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.3 状态切片 (State Slices)

```typescript
// slices/chatSlice.ts
interface ChatState {
  messages: Message[];
  isStreaming: boolean;
  streamContent: string;
}

const chatSlice = createSlice({
  name: 'chat',
  initialState,
  reducers: {
    addMessage: (state, action) => { /* ... */ },
    updateStreamContent: (state, action) => { /* ... */ },
    finishStreaming: (state) => { /* ... */ },
    clearChat: (state) => { /* ... */ },
  }
});

// slices/projectSlice.ts
interface ProjectState {
  projects: Project[];
  selectedId: string | null;
  viewMode: 'gantt' | 'table' | 'kanban';
}

// slices/configSlice.ts
interface ConfigState {
  provider: LLMProvider;
  apiConfig: ApiConfig;
  isValid: boolean;
}

// slices/uiSlice.ts
interface UIState {
  sidebarOpen: boolean;
  activeModal: string | null;
  theme: 'light' | 'dark';
}
```

---

## 4. 路由设计

### 4.1 路由结构

```typescript
// router/index.tsx
const routes = [
  {
    path: '/',
    element: <Layout />,
    children: [
      {
        index: true,
        element: <MainDashboard />
      },
      {
        path: 'project/:projectId',
        element: <ProjectDetail />
      },
      {
        path: 'gantt',
        element: <GanttView />
      },
      {
        path: 'gantt/:projectId',
        element: <GanttView />
      }
    ]
  },
  {
    path: '/config',
    element: <ConfigPage />
  },
  {
    path: '*',
    element: <NotFound />
  }
];
```

### 4.2 路由说明

| 路由 | 组件 | 说明 |
|------|------|------|
| `/` | MainDashboard | 主界面，显示聊天+项目面板 |
| `/project/:id` | ProjectDetail | 特定项目详情 |
| `/gantt` | GanttView | 所有项目的甘特图 |
| `/gantt/:id` | GanttView | 单个项目的甘特图 |
| `/config` | ConfigPage | 配置页面 |

### 4.3 路由守卫

```typescript
// 配置验证守卫
const ConfigGuard = ({ children }) => {
  const config = useSelector(state => state.config);
  const navigate = useNavigate();
  
  useEffect(() => {
    if (!config.isValid) {
      navigate('/config');
    }
  }, [config.isValid]);
  
  return children;
};
```

---

## 5. 关键组件Props接口定义

### 5.1 ChatPanel 组件

```typescript
// components/ChatPanel/types.ts

interface ChatPanelProps {
  /** 消息列表 */
  messages: Message[];
  /** 是否正在流式响应 */
  isStreaming: boolean;
  /** 流式内容 */
  streamContent?: string;
  /** 发送消息回调 */
  onSendMessage: (content: string, attachments?: File[]) => void;
  /** 停止生成回调 */
  onStopGeneration?: () => void;
  /** 重新生成回调 */
  onRegenerate?: (messageId: string) => void;
  /** 类名 */
  className?: string;
}

interface Message {
  id: string;
  role: 'user' | 'assistant' | 'system';
  content: string;
  timestamp: Date;
  attachments?: Attachment[];
  isError?: boolean;
}

interface Attachment {
  id: string;
  name: string;
  type: string;
  size: number;
  url: string;
}
```

### 5.2 ChatMessage 组件

```typescript
// components/ChatMessage/types.ts

interface ChatMessageProps {
  /** 消息数据 */
  message: Message;
  /** 是否显示头像 */
  showAvatar?: boolean;
  /** 是否为流式消息 */
  isStreaming?: boolean;
  /** 复制回调 */
  onCopy?: (content: string) => void;
  /** 重新生成回调 */
  onRegenerate?: () => void;
  /** 删除回调 */
  onDelete?: () => void;
}

interface MessageContentProps {
  /** 消息内容 */
  content: string;
  /** 是否渲染Markdown */
  enableMarkdown?: boolean;
  /** 代码高亮主题 */
  codeTheme?: string;
}
```

### 5.3 ChatInput 组件

```typescript
// components/ChatInput/types.ts

interface ChatInputProps {
  /** 输入值 */
  value: string;
  /** 输入变化回调 */
  onChange: (value: string) => void;
  /** 发送回调 */
  onSend: () => void;
  /** 文件上传回调 */
  onFileUpload?: (files: File[]) => void;
  /** 语音输入回调 */
  onVoiceInput?: () => void;
  /** 是否禁用 */
  disabled?: boolean;
  /** 占位符文本 */
  placeholder?: string;
  /** 最大输入长度 */
  maxLength?: number;
}
```

### 5.4 GanttChart 组件

```typescript
// components/GanttChart/types.ts

interface GanttChartProps {
  /** 项目数据 */
  projects: Project[];
  /** 选中的项目ID */
  selectedProjectId?: string;
  /** 时间范围 */
  timeRange: 'week' | 'month' | 'quarter';
  /** 时间范围变化回调 */
  onTimeRangeChange: (range: TimeRange) => void;
  /** 任务点击回调 */
  onTaskClick?: (task: Task) => void;
  /** 任务拖拽回调 */
  onTaskMove?: (taskId: string, newStart: Date, newEnd: Date) => void;
  /** 视图高度 */
  height?: number;
  /** 类名 */
  className?: string;
}

interface Project {
  id: string;
  name: string;
  startDate: Date;
  endDate: Date;
  progress: number;
  status: 'planning' | 'in_progress' | 'completed' | 'delayed';
  tasks: Task[];
}

interface Task {
  id: string;
  projectId: string;
  name: string;
  assignee: string;
  startDate: Date;
  endDate: Date;
  progress: number;
  status: TaskStatus;
  dependencies?: string[];
  deliverables?: Deliverable[];
}

type TaskStatus = 'todo' | 'in_progress' | 'review' | 'completed';
```

### 5.5 ProjectTable 组件

```typescript
// components/ProjectTable/types.ts

interface ProjectTableProps {
  /** 项目列表 */
  projects: Project[];
  /** 选中的项目ID */
  selectedId?: string;
  /** 排序配置 */
  sortConfig?: SortConfig;
  /** 选中回调 */
  onSelect?: (projectId: string) => void;
  /** 排序回调 */
  onSort?: (key: string, direction: 'asc' | 'desc') => void;
  /** 分页配置 */
  pagination?: PaginationConfig;
  /** 加载状态 */
  loading?: boolean;
}

interface SortConfig {
  key: string;
  direction: 'asc' | 'desc';
}

interface PaginationConfig {
  current: number;
  pageSize: number;
  total: number;
  onChange: (page: number, pageSize: number) => void;
}
```

### 5.6 ConfigForm 组件

```typescript
// components/ConfigForm/types.ts

interface ConfigFormProps {
  /** 初始配置 */
  initialConfig?: Partial<AppConfig>;
  /** 保存回调 */
  onSave: (config: AppConfig) => void;
  /** 测试连接回调 */
  onTestConnection?: (config: AppConfig) => Promise<boolean>;
  /** 取消回调 */
  onCancel?: () => void;
  /** 保存中状态 */
  saving?: boolean;
  /** 测试连接中状态 */
  testing?: boolean;
}

interface AppConfig {
  llmProvider: 'openai' | 'kimi' | 'doubao';
  apiConfig: {
    baseUrl: string;
    apiKey: string;
    model: string;
    temperature: number;
    maxTokens: number;
  };
}

// 提供商配置
const PROVIDER_CONFIGS = {
  openai: {
    name: 'OpenAI',
    defaultBaseUrl: 'https://api.openai.com/v1',
    models: ['gpt-4-turbo', 'gpt-4', 'gpt-3.5-turbo'],
    defaultModel: 'gpt-4-turbo'
  },
  kimi: {
    name: 'Kimi',
    defaultBaseUrl: 'https://api.moonshot.cn/v1',
    models: ['moonshot-v1-8k', 'moonshot-v1-32k', 'moonshot-v1-128k'],
    defaultModel: 'moonshot-v1-8k'
  },
  doubao: {
    name: '豆包',
    defaultBaseUrl: 'https://ark.cn-beijing.volces.com/api/v3',
    models: ['doubao-pro-4k', 'doubao-pro-32k', 'doubao-lite-4k'],
    defaultModel: 'doubao-pro-4k'
  }
};
```

### 5.7 DetailSidebar 组件

```typescript
// components/DetailSidebar/types.ts

interface DetailSidebarProps {
  /** 项目数据 */
  project: Project | null;
  /** 选中的任务 */
  selectedTask?: Task | null;
  /** 收起状态 */
  collapsed?: boolean;
  /** 收起切换回调 */
  onCollapseToggle?: () => void;
  /** 任务点击回调 */
  onTaskClick?: (task: Task) => void;
  /** 交付物点击回调 */
  onDeliverableClick?: (deliverable: Deliverable) => void;
}

interface Deliverable {
  id: string;
  name: string;
  type: 'document' | 'image' | 'video' | 'other';
  url: string;
  uploadedAt: Date;
  uploadedBy: string;
}
```

---

## 6. 样式规范

### 6.1 设计系统 (Design Tokens)

```typescript
// styles/tokens.ts

export const tokens = {
  // 颜色系统
  colors: {
    // 主色调
    primary: {
      50: '#eff6ff',
      100: '#dbeafe',
      200: '#bfdbfe',
      300: '#93c5fd',
      400: '#60a5fa',
      500: '#3b82f6',  // 主色
      600: '#2563eb',
      700: '#1d4ed8',
      800: '#1e40af',
      900: '#1e3a8a',
    },
    // 中性色
    neutral: {
      0: '#ffffff',
      50: '#f8fafc',
      100: '#f1f5f9',
      200: '#e2e8f0',
      300: '#cbd5e1',
      400: '#94a3b8',
      500: '#64748b',
      600: '#475569',
      700: '#334155',
      800: '#1e293b',
      900: '#0f172a',
    },
    // 状态色
    status: {
      success: '#22c55e',
      warning: '#f59e0b',
      error: '#ef4444',
      info: '#3b82f6',
    },
    // 项目状态色
    project: {
      planning: '#94a3b8',
      inProgress: '#3b82f6',
      completed: '#22c55e',
      delayed: '#ef4444',
    },
    // 任务状态色
    task: {
      todo: '#e2e8f0',
      inProgress: '#3b82f6',
      review: '#f59e0b',
      completed: '#22c55e',
    }
  },
  
  // 字体系统
  typography: {
    fontFamily: {
      sans: '-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif',
      mono: '"SF Mono", "Fira Code", "JetBrains Mono", monospace',
    },
    fontSize: {
      xs: '0.75rem',    // 12px
      sm: '0.875rem',   // 14px
      base: '1rem',     // 16px
      lg: '1.125rem',   // 18px
      xl: '1.25rem',    // 20px
      '2xl': '1.5rem',  // 24px
      '3xl': '1.875rem',// 30px
      '4xl': '2.25rem', // 36px
    },
    fontWeight: {
      normal: 400,
      medium: 500,
      semibold: 600,
      bold: 700,
    },
    lineHeight: {
      none: 1,
      tight: 1.25,
      normal: 1.5,
      relaxed: 1.625,
    }
  },
  
  // 间距系统
  spacing: {
    0: '0',
    1: '0.25rem',   // 4px
    2: '0.5rem',    // 8px
    3: '0.75rem',   // 12px
    4: '1rem',      // 16px
    5: '1.25rem',   // 20px
    6: '1.5rem',    // 24px
    8: '2rem',      // 32px
    10: '2.5rem',   // 40px
    12: '3rem',     // 48px
    16: '4rem',     // 64px
    20: '5rem',     // 80px
    24: '6rem',     // 96px
  },
  
  // 圆角系统
  borderRadius: {
    none: '0',
    sm: '0.125rem',  // 2px
    base: '0.25rem', // 4px
    md: '0.375rem',  // 6px
    lg: '0.5rem',    // 8px
    xl: '0.75rem',   // 12px
    '2xl': '1rem',   // 16px
    full: '9999px',
  },
  
  // 阴影系统
  shadows: {
    sm: '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
    base: '0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06)',
    md: '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
    lg: '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05)',
    xl: '0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04)',
    inner: 'inset 0 2px 4px 0 rgba(0, 0, 0, 0.06)',
  },
  
  // 过渡动画
  transitions: {
    fast: '150ms ease-in-out',
    base: '200ms ease-in-out',
    slow: '300ms ease-in-out',
  },
  
  // Z-index层级
  zIndex: {
    hide: -1,
    base: 0,
    docked: 10,
    dropdown: 1000,
    sticky: 1100,
    banner: 1200,
    overlay: 1300,
    modal: 1400,
    popover: 1500,
    skipLink: 1600,
    toast: 1700,
    tooltip: 1800,
  }
};
```

### 6.2 组件样式规范

```typescript
// styles/components.ts

// ChatPanel 样式
export const chatPanelStyles = {
  container: `
    w-[350px] 
    h-full 
    bg-white 
    border-r border-gray-200 
    flex flex-col
  `,
  messageList: `
    flex-1 
    overflow-y-auto 
    p-4 
    space-y-4
  `,
  inputArea: `
    p-4 
    border-t border-gray-200 
    bg-gray-50
  `,
};

// ChatMessage 样式
export const chatMessageStyles = {
  user: {
    container: 'flex justify-end',
    bubble: `
      max-w-[80%] 
      bg-blue-500 
      text-white 
      rounded-2xl rounded-tr-sm 
      px-4 py-2
    `,
  },
  assistant: {
    container: 'flex justify-start gap-3',
    avatar: 'w-8 h-8 rounded-full bg-gradient-to-br from-blue-400 to-purple-500',
    bubble: `
      max-w-[80%] 
      bg-gray-100 
      text-gray-800 
      rounded-2xl rounded-tl-sm 
      px-4 py-2
    `,
  },
};

// GanttChart 样式
export const ganttChartStyles = {
  container: 'bg-white rounded-lg shadow-sm border border-gray-200',
  toolbar: 'flex items-center justify-between p-4 border-b border-gray-200',
  timeline: 'overflow-x-auto',
  taskBar: {
    base: 'h-8 rounded-md relative cursor-pointer transition-all',
    planning: 'bg-gray-400',
    inProgress: 'bg-blue-500',
    completed: 'bg-green-500',
    delayed: 'bg-red-500',
  },
};

// ProjectTable 样式
export const projectTableStyles = {
  container: 'bg-white rounded-lg shadow-sm border border-gray-200 overflow-hidden',
  header: 'bg-gray-50 border-b border-gray-200',
  headerCell: 'px-4 py-3 text-left text-sm font-semibold text-gray-700',
  row: 'border-b border-gray-100 hover:bg-gray-50 transition-colors',
  cell: 'px-4 py-3 text-sm text-gray-600',
  progressBar: 'h-2 bg-gray-200 rounded-full overflow-hidden',
  progressFill: 'h-full bg-blue-500 rounded-full transition-all',
};
```

### 6.3 CSS变量定义

```css
/* styles/variables.css */

:root {
  /* 颜色变量 */
  --color-primary-50: #eff6ff;
  --color-primary-500: #3b82f6;
  --color-primary-600: #2563eb;
  
  --color-neutral-0: #ffffff;
  --color-neutral-100: #f1f5f9;
  --color-neutral-200: #e2e8f0;
  --color-neutral-700: #334155;
  --color-neutral-900: #0f172a;
  
  --color-success: #22c55e;
  --color-warning: #f59e0b;
  --color-error: #ef4444;
  
  /* 布局变量 */
  --header-height: 60px;
  --chat-panel-width: 350px;
  --detail-sidebar-width: 300px;
  
  /* 间距变量 */
  --spacing-xs: 0.25rem;
  --spacing-sm: 0.5rem;
  --spacing-md: 1rem;
  --spacing-lg: 1.5rem;
  --spacing-xl: 2rem;
  
  /* 圆角变量 */
  --radius-sm: 0.25rem;
  --radius-md: 0.375rem;
  --radius-lg: 0.5rem;
  --radius-xl: 0.75rem;
  
  /* 阴影变量 */
  --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
  --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
  --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
  
  /* 过渡变量 */
  --transition-fast: 150ms ease-in-out;
  --transition-base: 200ms ease-in-out;
}

/* 暗黑模式 */
[data-theme='dark'] {
  --color-neutral-0: #0f172a;
  --color-neutral-100: #1e293b;
  --color-neutral-200: #334155;
  --color-neutral-700: #cbd5e1;
  --color-neutral-900: #f8fafc;
}
```

---

## 7. 交互流程说明

### 7.1 聊天交互流程

```
┌─────────────────────────────────────────────────────────────────┐
│                      聊天交互流程                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  用户输入                                                       │
│     │                                                           │
│     ▼                                                           │
│  ┌─────────────────┐                                            │
│  │ 1. 输入验证     │ 检查内容非空、长度限制                      │
│  └────────┬────────┘                                            │
│           │                                                     │
│           ▼                                                     │
│  ┌─────────────────┐                                            │
│  │ 2. 添加用户消息 │ 添加到消息列表，清空输入框                  │
│  └────────┬────────┘                                            │
│           │                                                     │
│           ▼                                                     │
│  ┌─────────────────┐                                            │
│  │ 3. 显示加载状态 │ 显示打字指示器                              │
│  └────────┬────────┘                                            │
│           │                                                     │
│           ▼                                                     │
│  ┌─────────────────┐      ┌─────────────────┐                  │
│  │ 4. 发送请求     │ ───▶ │ API (SSE流式)   │                  │
│  └────────┬────────┘      └────────┬────────┘                  │
│           │                        │                            │
│           │                        ▼                            │
│           │               ┌─────────────────┐                   │
│           │               │ 5. 流式响应     │                   │
│           │               │ 逐字显示内容    │                   │
│           │               └────────┬────────┘                   │
│           │                        │                            │
│           ▼                        ▼                            │
│  ┌─────────────────────────────────────────┐                   │
│  │ 6. 完成响应 / 错误处理                  │                   │
│  │    - 保存完整消息                       │                   │
│  │    - 隐藏加载状态                       │                   │
│  │    - 显示错误提示(如有)                 │                   │
│  └─────────────────────────────────────────┘                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 7.2 甘特图交互流程

```
┌─────────────────────────────────────────────────────────────────┐
│                     甘特图交互流程                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  初始化加载                                                     │
│     │                                                           │
│     ▼                                                           │
│  ┌─────────────────┐                                            │
│  │ 1. 获取项目数据 │ 从API获取所有项目/任务数据                 │
│  └────────┬────────┘                                            │
│           │                                                     │
│           ▼                                                     │
│  ┌─────────────────┐                                            │
│  │ 2. 计算时间范围 │ 根据项目起止时间计算显示范围               │
│  └────────┬────────┘                                            │
│           │                                                     │
│           ▼                                                     │
│  ┌─────────────────┐                                            │
│  │ 3. 渲染甘特图   │ 渲染时间轴、任务条、依赖关系               │
│  └────────┬────────┘                                            │
│           │                                                     │
│     ┌─────┴─────┬─────────────┬─────────────┐                  │
│     │           │             │             │                  │
│     ▼           ▼             ▼             ▼                  │
│ ┌───────┐  ┌────────┐  ┌──────────┐  ┌──────────┐             │
│ │时间   │  │任务条  │  │视图切换  │  │拖拽调整  │             │
│ │范围   │  │点击    │  │          │  │          │             │
│ │切换   │  │        │  │          │  │          │             │
│ └───┬───┘  └───┬────┘  └────┬─────┘  └────┬─────┘             │
│     │          │            │             │                    │
│     ▼          ▼            ▼             ▼                    │
│ ┌─────────────────────────────────────────────────┐           │
│ │ 4. 更新视图 / 显示详情 / 保存更改               │           │
│ └─────────────────────────────────────────────────┘           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 7.3 配置保存流程

```
┌─────────────────────────────────────────────────────────────────┐
│                     配置保存流程                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  用户修改配置                                                   │
│     │                                                           │
│     ▼                                                           │
│  ┌─────────────────┐                                            │
│  │ 1. 表单验证     │ 验证必填项、URL格式、API Key格式          │
│  └────────┬────────┘                                            │
│           │                                                     │
│           ▼                                                     │
│  ┌─────────────────┐                                            │
│  │ 2. 测试连接     │ (可选) 发送测试请求验证配置               │
│  └────────┬────────┘                                            │
│           │                                                     │
│     ┌─────┴─────┐                                               │
│     │           │                                               │
│     ▼           ▼                                               │
│ ┌───────┐  ┌────────┐                                           │
│ │ 成功  │  │ 失败   │                                           │
│ └───┬───┘  └────┬───┘                                           │
│     │           │                                               │
│     ▼           ▼                                               │
│ ┌─────────────────┐  ┌─────────────────┐                       │
│ │ 3. 保存到本地   │  │ 显示错误提示    │                       │
│ │ localStorage    │  │ 高亮错误字段    │                       │
│ └────────┬────────┘  └─────────────────┘                       │
│          │                                                      │
│          ▼                                                      │
│ ┌─────────────────┐                                             │
│ │ 4. 更新全局状态 │ 更新Redux Store中的配置                    │
│ └────────┬────────┘                                             │
│          │                                                      │
│          ▼                                                      │
│ ┌─────────────────┐                                             │
│ │ 5. 显示成功提示 │ Toast提示配置已保存                        │
│ └─────────────────┘                                             │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 7.4 项目切换流程

```
┌─────────────────────────────────────────────────────────────────┐
│                     项目切换流程                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  用户选择项目                                                   │
│     │                                                           │
│     ▼                                                           │
│  ┌─────────────────┐                                            │
│  │ 1. 更新选中状态 │ 更新UI中的选中状态                        │
│  └────────┬────────┘                                            │
│           │                                                     │
│           ▼                                                     │
│  ┌─────────────────┐                                            │
│  │ 2. 获取详情数据 │ 加载项目详情、任务列表、交付物            │
│  └────────┬────────┘                                            │
│           │                                                     │
│           ▼                                                     │
│  ┌─────────────────┐                                            │
│  │ 3. 更新甘特图   │ 高亮选中项目的任务条                      │
│  └────────┬────────┘                                            │
│           │                                                     │
│           ▼                                                     │
│  ┌─────────────────┐                                            │
│  │ 4. 更新详情面板 │ 显示项目信息和子任务列表                  │
│  └────────┬────────┘                                            │
│           │                                                     │
│           ▼                                                     │
│  ┌─────────────────┐                                            │
│  │ 5. 更新URL      │ 可选: 更新路由 /project/:id               │
│  └─────────────────┘                                            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 8. 响应式设计细节

### 8.1 断点布局变化

```
Desktop (≥1440px):
┌─────────────────────────────────────────────────────────────────┐
│ Header                                                          │
├───────────────┬───────────────────────────────┬─────────────────┤
│ Chat (350px)  │      Project Panel            │ Detail (300px)  │
│               │      (flex: 1)                │                 │
└───────────────┴───────────────────────────────┴─────────────────┘

Tablet (768px-1439px):
┌─────────────────────────────────────────────────────────────────┐
│ Header                                          [≡] [Detail ▶]  │
├─────────────────────────────────────────────────────────────────┤
│ Chat (280px) │           Project Panel (flex: 1)               │
│ [可收起]     │                                                  │
└──────────────┴──────────────────────────────────────────────────┘

Mobile (<768px):
┌─────────────────────────────────────────────────────────────────┐
│ Header                                          [≡]             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│                     Project Panel                               │
│                     (全屏显示)                                  │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│ [💬 Chat]  [📊 项目]  [📅 甘特图]  [⚙️ 设置]                    │
└─────────────────────────────────────────────────────────────────┘
```

### 8.2 移动端适配要点

| 组件 | 适配方案 |
|------|----------|
| ChatPanel | 底部Tab切换，全屏弹窗 |
| GanttChart | 横向滚动，简化任务条 |
| ProjectTable | 卡片列表替代表格 |
| DetailSidebar | 底部抽屉弹出 |
| ConfigForm | 分步骤表单，每屏一个section |

---

## 9. 可访问性 (Accessibility) 规范

### 9.1 ARIA标签

```tsx
// 聊天消息
<div role="log" aria-live="polite" aria-label="聊天消息">
  <article aria-label="用户消息">...</article>
  <article aria-label="AI回复">...</article>
</div>

// 甘特图
<div role="region" aria-label="项目甘特图">
  <div role="list" aria-label="任务列表">
    <div role="listitem" aria-label="任务: 任务名称">...</div>
  </div>
</div>

// 配置表单
<form aria-label="系统配置">
  <fieldset>
    <legend>LLM提供商</legend>
    <input type="radio" aria-label="OpenAI" />
  </fieldset>
</form>
```

### 9.2 键盘导航

| 快捷键 | 功能 |
|--------|------|
| `Tab` | 在可交互元素间切换 |
| `Enter` | 激活按钮/链接 |
| `Esc` | 关闭弹窗/取消操作 |
| `Ctrl/Cmd + Enter` | 发送消息 |
| `Ctrl/Cmd + K` | 打开命令面板 |
| `Ctrl/Cmd + /` | 显示快捷键帮助 |

---

## 10. 性能优化建议

### 10.1 渲染优化

```typescript
// 1. 使用 React.memo 避免不必要重渲染
const ChatMessage = React.memo(({ message, ...props }) => {
  // ...
});

// 2. 使用 useMemo 缓存计算结果
const sortedProjects = useMemo(() => {
  return projects.sort((a, b) => a.startDate - b.startDate);
}, [projects]);

// 3. 使用 useCallback 缓存回调函数
const handleSendMessage = useCallback((content) => {
  dispatch(sendMessage(content));
}, [dispatch]);

// 4. 虚拟列表处理大量消息
import { VirtualList } from 'react-window';
<VirtualList
  height={500}
  itemCount={messages.length}
  itemSize={80}
  renderItem={MessageItem}
/>
```

### 10.2 数据加载优化

| 优化策略 | 实现方式 |
|----------|----------|
| 懒加载 | 项目列表分页加载 |
| 预加载 | 鼠标悬停时预加载项目详情 |
| 缓存 | React Query缓存项目数据 |
| 防抖 | 搜索输入300ms防抖 |
| 节流 | 甘特图拖拽16ms节流 |

---

## 11. 技术栈推荐

### 11.1 核心依赖

```json
{
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.0",
    "@reduxjs/toolkit": "^2.0.0",
    "react-redux": "^9.0.0",
    
    "@tanstack/react-query": "^5.0.0",
    "axios": "^1.6.0",
    
    "react-markdown": "^9.0.0",
    "remark-gfm": "^4.0.0",
    "prismjs": "^1.29.0",
    
    "date-fns": "^3.0.0",
    "lodash-es": "^4.17.21",
    
    "tailwindcss": "^3.3.0",
    "class-variance-authority": "^0.7.0",
    "clsx": "^2.0.0",
    "tailwind-merge": "^2.0.0"
  }
}
```

### 11.2 甘特图库推荐

| 库名 | 优点 | 缺点 |
|------|------|------|
| `gantt-task-react` | React原生、易定制 | 功能较简单 |
| `frappe-gantt` | 轻量、性能好 | 需适配React |
| `@mermaid-js/mermaid` | 支持多种图表 | 甘特图功能有限 |
| 自定义实现 | 完全可控 | 开发成本高 |

---

## 12. 文件结构建议

```
src/
├── components/              # 组件目录
│   ├── ui/                 # 基础UI组件
│   │   ├── Button/
│   │   ├── Input/
│   │   ├── Select/
│   │   └── Modal/
│   ├── chat/               # 聊天相关组件
│   │   ├── ChatPanel/
│   │   ├── ChatMessage/
│   │   ├── ChatInput/
│   │   └── TypingIndicator/
│   ├── project/            # 项目相关组件
│   │   ├── ProjectTable/
│   │   ├── ProjectCard/
│   │   └── ProjectSelector/
│   ├── gantt/              # 甘特图组件
│   │   ├── GanttChart/
│   │   ├── GanttTaskBar/
│   │   └── GanttTimeline/
│   ├── kanban/             # 看板组件
│   │   ├── KanbanBoard/
│   │   ├── KanbanColumn/
│   │   └── KanbanCard/
│   └── layout/             # 布局组件
│       ├── Header/
│       ├── Sidebar/
│       └── MainLayout/
├── hooks/                  # 自定义Hooks
│   ├── useChat.ts
│   ├── useProjects.ts
│   ├── useGantt.ts
│   └── useConfig.ts
├── store/                  # 状态管理
│   ├── index.ts
│   ├── slices/
│   │   ├── chatSlice.ts
│   │   ├── projectSlice.ts
│   │   ├── configSlice.ts
│   │   └── uiSlice.ts
│   └── middleware/
├── services/               # API服务
│   ├── api.ts
│   ├── chatService.ts
│   ├── projectService.ts
│   └── configService.ts
├── types/                  # TypeScript类型
│   ├── chat.ts
│   ├── project.ts
│   ├── config.ts
│   └── index.ts
├── utils/                  # 工具函数
│   ├── dateUtils.ts
│   ├── formatUtils.ts
│   └── validationUtils.ts
├── styles/                 # 样式文件
│   ├── globals.css
│   ├── variables.css
│   └── components.css
├── router/                 # 路由配置
│   └── index.tsx
└── App.tsx
```

---

## 总结

本文档提供了项目管理助手机器人前端界面的完整设计方案，包括：

1. **整体布局**：三栏式主界面 + 配置页面
2. **组件结构**：清晰的组件层级和关系
3. **状态管理**：Redux Toolkit分slice管理
4. **路由设计**：简洁的路由结构
5. **Props接口**：详细的TypeScript类型定义
6. **样式规范**：完整的设计系统
7. **交互流程**：用户操作的完整流程图

设计遵循现代UI/UX最佳实践，支持响应式布局，注重可访问性和性能优化。
